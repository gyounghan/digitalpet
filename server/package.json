{
  "name": "pocketfriend-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "PocketFriend 서버 — 펫 클라우드 저장/기기 이전, 계정(이메일·카카오·네이버), 실시간 배틀, 랭킹",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "node --test",
    "test:integration": "node test/integration_battle.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "socket.io": "^4.7.5"
  },
  "devDependencies": {
    "socket.io-client": "^4.8.3"
  }
}
